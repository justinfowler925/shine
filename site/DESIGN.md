# Shine public page — DESIGN.md

**Lane:** marketing · **Job:** get a builder to clone the skill · **Cite:** `magicui-hero`
**Voice:** kit-faithful (not house — house is the cyan/serif essay they already have)
**Unlock:** full rewrite. The notes layout is the defect.

## Subject

A Cursor/Claude user whose generated UI looks like everyone else’s. Problem-aware.

## Regions (`corpus/packs/magicui-hero/regions.json`)

```
[  SHINE (brand in the hero, not only nav)          ]
[  headline · one lede · one filled CTA             ]  hero ~55%
[  ONE media: kit-faithful Carbon queue             ]
[  proof: the costume V1 passed · what to install   ]  proof ~25%
```

No two-up collage. No 12-section memoir. No KPI row.

## Tokens

| Role | Value |
|---|---|
| canvas | near-black `oklch(0.12 0.004 260)` / paper `oklch(0.99 0.002 260)` — not cream |
| primary | inverted fill (white on dark, ink on light) — specimen button |
| display | Syne 500, 48–72px, tracking −0.03em |
| body | system 16 / 1.4 |
| radius | control 12 · card 20 · media 24 |
| motion | 300ms |
| glow | white 8% radial behind type — Magic UI elevation, not cyan, not indigo |

No house cyan. No second accent.

## Signature

Type as identity on a black field, one product shot, white primary. Chanel: the cyan is gone.

## Uniqueness

Not cream+serif+terracotta, not OLED+acid-green, not broadsheet, not indigo-on-zinc.
Not the previous house essay with a bigger headline.

## Copy (beliefs)

1. Outcome: the agent fails a Carbon cite it painted as zinc.
2. Proof: costume likeness 2 next to the claim; queue is the hero media.
3. Time: clone, symlink, `doctor.mjs` — same afternoon.
4. Effort: one git directory, two surfaces.
5. Trust: MIT, doctor `--ci` count from the run, skill tree from disk.
