The public catalog is an application browser. Its primary structural reference is shadcn-sidebar-07: persistent navigation, an inset content region and responsive navigation. The dashboard example separately follows shadcn-dashboard-01: summary, chart and source ledger. Both upstream pages were recaptured on Studio with HTTP/content evidence. The packet initially selected the dashboard for the broad combined brief; the catalog itself does not claim to be a dashboard.

The existing public fixture had no usable catalog precedent. The gallery theme belongs to this consumer and does not overwrite Nucleus tokens. Build integrity verifies all 32 registry imports. Explicit shine-project.json source roots allow Shine to verify its own nonstandard repository layout instead of silently reporting zero reusable components.
