# e2e · Brutus session

Lane: **internal**. Job: rescue a live Brutus chat+board session into a kit-faithful after page.

Cite: **shadcn-dashboard-01** (sidebar, four KPI cards, area chart, data table). Voice: kit-faithful `shadcn-zinc`.

`before.png` is a live capture of `http://127.0.0.1:8768/session` (not a fixture). Measure on that URL failed: 3 competing filled treatments, axe `link-in-text-block`, accent reused as stage chrome, 555-item kanban as the focal.

`after.html` clones dashboard-01 regions. Table contract: sort, pagination, empty/loading/error. One filled primary (Capture).

Measured after: axe 0, contrast worst 4.83:1 (40 text), theme switches, 1 filled treatment. `compare.mjs --cite shadcn-dashboard-01` exit 0.
